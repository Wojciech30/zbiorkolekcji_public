{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "jsonwebtoken": "^9.0.2",
    "mongodb": "^6.13.0",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "build:css": "tailwindcss build src/tailwind.css -o public/tailwind.css --minify",
    "start": "concurrently \"npm run serve --prefix frontend\" \"node backend/server.js\""
  }
}
